# .github/workflows/build-native-arm64.yml
name: Build Spring Boot Native ARM64

on:
  # Ejecutar en push a main/master
  #push:
  #  branches: [ main, master ]
  #  paths-ignore:
  #    - 'README.md'
  #    - 'docs/**'
  
  # Ejecutar en pull requests
  #pull_request:
  #  branches: [ main, master ]
  
  # Permitir ejecución manual
  workflow_dispatch:
    inputs:
      skip_tests:
        description: 'Skip tests during build'
        required: false
        default: 'false'
        type: boolean

jobs:
  build-arm64:
    runs-on: ubuntu-24.04-arm      # runner ARM64
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Get Project Version from POM
        id: get_version
        run: echo "VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_OUTPUT      

      - name: Cache Maven Dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Setup GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '21'
          distribution: 'graalvm-community'

      - name: Build native (AOT)
        #run: mvn -Pnative -DskipTests -Dspring.aot.enabled=true -B package
        run: mvn -Pnative -DskipTests -Dspring.aot.enabled=true -B native:compile

      - name: Upload native artifact
        uses: actions/upload-artifact@v4
        with:
          name: pipool-linux-aarch64
          path: target/pipool

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.get_version.outputs.VERSION }}
          name: Release v${{ steps.get_version.outputs.VERSION }}
          body: |
            Automated release for version ${{ steps.get_version.outputs.VERSION }}
            Changes can be found in the commits since the last release.
          draft: false
          prerelease: false
          files: target/pipool